{
  "name": "netplus-trainer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "An interactive browser game for studying CompTIA Network+ (N10-009), built to accept verified content from published study guides.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run test"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  }
}
